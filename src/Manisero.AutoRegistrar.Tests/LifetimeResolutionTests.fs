module LifetimeResolutionTests

open System
open Xunit
open FsUnit.Xunit
open LifetimeResolution

// test data


// helpers

// tests

[<Fact>]
let ``already resolved -> existing registration``() =
    raise (NotImplementedException())

[<Fact>]
let ``no dependencies -> longestLifetime``() = 
    raise (NotImplementedException())

[<Fact>]
let ``single dependency -> derived``() = 
    raise (NotImplementedException())
